//   remove the value from the beginning of the array its similar to the pop method

const num = [1, 2, 3, 4, 5];

num.shift(); //          it will remove the value from beginning

num.shift(); //          it will remove the value from beginning

console.log(num);

//     if you want to find the removed value just do this way

const nums = [1, 2, 3, 4, 5];

const removedvalue1 = nums.shift(); //          it will remove the value from beginning

const removedvalue2 = nums.shift(); //          it will remove the value from beginning

console.log();

//    now you will get the removed value

console.log(removedvalue1);

console.log(removedvalue2);
