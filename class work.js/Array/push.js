//     add the value at last position of the array

const num = [1, 2, 3];

const res = num.push(4, 5, 6);

console.log(num); //        we can get pushed array

console.log(res); //        in this case we can get length of the  array

//                so whatever you to push just go to this way

const nums = [1, 2, 3];

nums.push(4, 5, 6); //      now you can get the array with pushed value

console.log(nums);
